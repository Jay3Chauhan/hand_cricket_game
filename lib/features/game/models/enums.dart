/// Game phase enum to track the current stage of the game
enum GamePhase {
  notStarted,
  userBatting,
  botBatting,
  gameOver,
}

/// Player enum to identify the player type
enum Player {
  user,
  bot,
}
